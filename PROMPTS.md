## Prompt 1

1. As an Software Engineer, you need to design a scalable mvc model for a driver registeration process, along with a simple dashboard with the aggregated data of those drivers' registeration status. The stack use to implement this is js and sqlite, I need a Fluent module to simplify the usage of sqlite3, and called by ORM(follow OOD pattern). After that, I need controller to render ejs view, and a general router for all the page request. Make the file structure without pumping too much actual bussiness logics code for now.

2. And I reviewed and twick the AI generated code, make sure the project architecture meets my expectation.

